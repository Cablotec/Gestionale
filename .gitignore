# Il repo è PUBBLICO: qui dentro non deve finire niente di riservato.
# I backup dei dati stanno FUORI dal repo (vedi strumenti/backup.js), ma se
# qualcuno un domani li scaricasse qui, questa è la seconda rete.
backup-gestionale/
backup/
*.backup.json
# Dump di lavoro del caricatore distinte: dati di produzione, non entrano.
carico-*.json
materiali-commesse.sql
segreti*
*.env
.env*
strumenti/materializza-distinte.json
